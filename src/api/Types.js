import FreightPacker, { InitializationParams } from "../FreightPacker";
import BoxEntry from "./components/box/BoxEntry";
import { SolverParams, CUBParams } from "./components/PackerInterface";

/** @typedef {FreightPacker} FreightPacker */
/** @typedef {InitializationParams} InitializationParams */
/** @typedef {SolverParams} SolverParams */
/** @typedef {CUBParams} CUBParams */
/** @typedef {BoxEntry} BoxEntry */

/**
 * @param {CUBParams} v
 */
function f(v){

}
